{
  "name": "@contrastagent/logger",
  "version": "0.0.0",
  "license": "MIT",
  "source": "src/index.ts",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "watch": "tsup --watch",
    "lint": "eslint \"src/**/*.ts*\""
  },
  "devDependencies": {
    "@contrastagent/eslint-config": "workspace:*",
    "@contrastagent/typescript-config": "workspace:*",
    "tsup": "^8.1.0"
  }
}
