#!/bin/bash
# This script runs the agent application for getting the latest product data from vendor sites.
# It assumes that the environment is already set up and the necessary dependencies are installed.
python apps/api/app/agent/app.py
